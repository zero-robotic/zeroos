syntax = "proto3";

package zos.geometry;

import "geometry_msgs/twist.proto";
import "std_msgs/header.proto";

// Twist with reference coordinate frame and timestamp.
message TwistStamped {
  zos.std.Header header = 1;
  Twist twist = 2;
}
